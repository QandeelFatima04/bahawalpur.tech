# Local dev outside docker: hit the API directly from the browser.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# In docker, leave NEXT_PUBLIC_API_BASE empty and let Next.js rewrite to:
# API_INTERNAL_URL=http://api:8000
